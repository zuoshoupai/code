### 京东开发票脚本
> 操作步骤:1,打开地址  https://myivc.jd.com/fpzz/index.action#none 2，申请发票或换开发票 进入页面 3,F12,粘贴以下全部代码 回车执行
```html

var data={};
data.mobile='15373850656';
data.email='245754047@qq.com';
data.recive='李则';
data.company='北京森倍思科技有限公司';
data.tax='9111010808964870X9';
data.register='北京市海淀区西土城路10号21号楼214室';
data.address_detail='西土城路10号21号楼214室';
data.phone='82600818';
data.bank='中国农业银行股份有限公司北京新外支行';
data.account='11020301040010793';
function update_obj(name,show,val=false){
	if(document.getElementById(name)!=null){
		if(name=='countyNum'){
			document.getElementById(name).options.add(new Option("三环以内", "2848"));
		}
		if(show){
			document.getElementById(name).style.display='block';
		}
		if(val){
			document.getElementById(name).value=val;
		}
	}
}
update_obj('ivcTitle',1,'单位');
update_obj('companyDiv',1);
update_obj('taxNoDiv',1);
update_obj('companyNameDiv',1);
update_obj('eiRegistAddressDiv',1);
update_obj('eiRegistPhoneDiv',1);
update_obj('eiBankDiv',1);
update_obj('eiBankAccountDiv',1);
update_obj('company',0,data.company);
update_obj('companyName',0,data.company);
update_obj('taxNo',0,data.tax);
update_obj('eiRegistAddress',0,data.register);
update_obj('eiRegistPhone',0,data.phone);
update_obj('eiBank',0,data.bank);
update_obj('eiBankAccount',0,data.account);
update_obj('customerTelE',0,data.mobile);
update_obj('customerTel',0,data.mobile);
update_obj('email',0,data.email);
update_obj('customerName',0,data.recive); 
update_obj('eiBankAccount',0,data.account);
update_obj('address',0,data.address_detail);
update_obj('provinceNum',0,1);
update_obj('cityNum',0,2800);
setTimeout(function(){
	update_obj('countyNum',0,2848);
},300) 
if(document.getElementById('ivcContentSpan100')!=null){
	document.getElementById('ivcContentSpan100').click();
} 
$('.f-btnbox a.u-btn-primary').trigger('click');
```
